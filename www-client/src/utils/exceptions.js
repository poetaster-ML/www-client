/*
const assert = assertions => (
  target, key, descriptor
) => (...args) {
  for (let i = 0; i < assertions.length; i++) {
    const [test, msg] = assertions[i];
    if (!test.apply(null, args)) {
      throw new Error(msg);
    }
  }
  return target(...args);
};

export {
  assert
};

*/
